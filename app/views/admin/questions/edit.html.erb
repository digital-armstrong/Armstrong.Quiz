<div class="max-w-xl">
  <h1 class="text-3xl font-bold mb-6"><%= t("admin.questions.edit") %></h1>
  <%= render "form", question: @question, url: admin_question_path(@question) %>
  <%= link_to t("admin.actions.back"), admin_question_path(@question), class: "btn btn-ghost mt-4" %>
</div>
