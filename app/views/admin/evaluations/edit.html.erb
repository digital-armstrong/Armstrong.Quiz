<div class="max-w-xl">
  <h1 class="text-2xl font-bold mb-6"><%= t("admin.evaluations.edit_title") %></h1>
  <%= render "form", evaluation: @evaluation, user: @evaluation.user %>
  <%= link_to t("admin.actions.back"), admin_result_path(@evaluation.user_id), class: "btn btn-ghost mt-4" %>
</div>
