<div class="max-w-xl">
  <h1 class="text-3xl font-bold mb-6"><%= t("admin.categories.new") %></h1>
  <%= render "form", category: @category, url: admin_categories_path %>
  <%= link_to t("admin.actions.back"), admin_categories_path, class: "btn btn-ghost mt-4" %>
</div>
