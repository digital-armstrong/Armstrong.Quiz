<%= turbo_frame_tag "quiz_question" do %>
  <div class="max-w-2xl mx-auto card bg-base-100 shadow-xl">
    <div class="card-body text-center">
      <h2 class="card-title text-2xl justify-center"><%= t("quiz.thank_you_title") %></h2>
      <p class="text-base-content/80"><%= t("quiz.thank_you_message") %></p>
      <%= link_to t("quiz.back_home"), root_path, class: "btn btn-primary mt-4", data: { turbo_frame: "_top" } %>
    </div>
  </div>
<% end %>
